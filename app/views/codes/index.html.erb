	<article class="clearfix">
		<p><em><%= @codes.total_entries %> ICD-9-CM Diagnosis Codes Found</em></p>
		
		<%= will_paginate @codes %>
		
		<ol class="searchResults">

			<% @codes.each do |code| %>
  			<li>
				<%= link_to raw('<h3>'+code.code+'</h3>'+'<p>'+code.short+'</p>'), code %>
			</li>
			<% end %>
			
		</ol>
	</article>
